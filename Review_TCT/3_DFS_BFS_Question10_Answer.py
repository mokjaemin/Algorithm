

# 블록 이동하기
from collections import deque

def get_next_pos(pos, board):
    next_pos = [] # 이동 가능한 위치들
    pos = list(pos) # 현재 위치 정보를 리스트로 변환(집합 - 리스트)

def solution(board):
    n = len(board)
    new_board = [[1]]